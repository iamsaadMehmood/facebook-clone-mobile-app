import {Text} from 'native-base';
import Layout from '../components/Layout';

const LoginScreen = () => {
  return (
    <Layout>
      <Text>Login</Text>
    </Layout>
  );
};

export default LoginScreen;
